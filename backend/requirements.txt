# ReproLab backend — pinned runtime dependencies.
# Source of truth: ../pyproject.toml. Keep in sync.
# Install with: pip install -r backend/requirements.txt
# (For development tooling, also install backend/requirements-dev.txt.)

# --- Core: HTTP server, async, validation -----------------------------------
anyio>=4,<5
fastapi>=0.111,<1
python-multipart>=0.0.9,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.7,<3
pydantic-settings>=2.2,<3
httpx>=0.27,<1

# --- Ingestion: PDF parsing -------------------------------------------------
beautifulsoup4>=4.12,<5
Pillow>=10,<12
pymupdf>=1.24,<2
pytesseract>=0.3.10,<0.4

# --- Agent layer: provider SDKs ---------------------------------------------
claude-agent-sdk>=0.1.80
openai-agents>=0.17,<0.18

# --- Sandbox runtime --------------------------------------------------------
docker>=7,<8
asyncssh>=2.14,<3

# --- Azure compute sandbox (--sandbox azure) --------------------------------
# Imported lazily inside azure_backend.py when the Azure sandbox is selected.
# Listed as runtime deps so unit tests can monkeypatch the SDK without an
# ImportError. No `azure-cli` dependency — we do not shell out.
azure-identity>=1.16,<2
azure-mgmt-compute>=30,<32
azure-mgmt-network>=25,<27
azure-mgmt-resource>=23,<25

# --- Evaluation: reproduction + innovation scoring --------------------------
# Required by backend/evals/* — install if you use the `eval` CLI.
deepeval>=2.0
scipy>=1.12
numpy>=1.26

# --- RLM orchestrator -------------------------------------------------------
rlms==0.1.1
python-dotenv>=1.2.1,<2
